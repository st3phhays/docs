---
import { getCollection } from 'astro:content';

import Highlight from '@components/highlights/Highlight.astro';

const highlightsHome = await getCollection('docs', ({ data }) => {
    return data.highlight && data.highlight.showOnHome;
});
---

<Highlight content={highlightsHome} />
